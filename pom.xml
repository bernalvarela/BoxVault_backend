<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.storagemanager</groupId>
	<artifactId>boxVault-backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name/>
	<description/>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>25</java.version>
		<aws.sdk.version>2.54.9</aws.sdk.version>
	</properties>

	<!--
	  El SDK de AWS no lo gestiona el parent de Spring Boot: su BOM fija aquí la
	  versión de todos sus artefactos (s3, url-connection-client y lo que arrastren).
	-->
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>software.amazon.awssdk</groupId>
				<artifactId>bom</artifactId>
				<version>${aws.sdk.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-h2console</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>

		<!--
		  Autenticación y autorización. Los JWT los emite y valida el propio Spring
		  Security con Nimbus (lo que trae oauth2-resource-server), sin jjwt ni
		  filtros a mano: es lo que la imagen nativa lleva probado, con sus pistas
		  de AOT. El día que se quiera un proveedor externo (Keycloak y compañía)
		  basta con cambiar el emisor: lo que valida los tokens no cambia.
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>

		<!--
		  Cliente S3 para el almacén de ficheros adjuntos (RustFS en el servidor).
		  Se excluyen los dos clientes HTTP que trae por defecto, Netty y Apache, y se
		  usa url-connection-client: el HTTP del propio JDK, sin hilos ni pool propios
		  y con muchas menos aristas en la imagen nativa. El SDK trae sus metadatos de
		  GraalVM, así que no hace falta registrar nada en NativeHints.
		-->
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>s3</artifactId>
			<exclusions>
				<exclusion>
					<groupId>software.amazon.awssdk</groupId>
					<artifactId>netty-nio-client</artifactId>
				</exclusion>
				<exclusion>
					<groupId>software.amazon.awssdk</groupId>
					<artifactId>apache-client</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>url-connection-client</artifactId>
		</dependency>

		<!--
		  Migraciones de la base de datos. Las lanza la propia aplicación al arrancar,
		  en orden y una sola vez, y anota en flyway_schema_history cuáles ha aplicado:
		  así un despliegue no depende de que alguien se acuerde de pasar un .sql por
		  psql. Sólo se activa con el perfil "pro"; en desarrollo la base es H2, se crea
		  de cero en cada arranque y estas migraciones son de PostgreSQL.

		  flyway-database-postgresql va aparte desde Flyway 10: el núcleo ya no trae
		  dentro el soporte de cada motor.
		-->
		<!--
		  El arranque automático de Flyway. En Spring Boot 4 la autoconfiguración
		  se partió en módulos, así que flyway-core trae la biblioteca pero NO
		  quien la pone en marcha al arrancar: sin esto, Flyway está en el
		  classpath y no se ejecuta jamás, sin un aviso ni una línea de log.
		  Es lo que pasó durante semanas, y el mismo patrón que spring-boot-h2console.
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!--
		  PDF de las facturas y de los contratos. OpenPDF es el iText 4 libre (LGPL/MPL),
		  sin las restricciones de licencia de iText 5+, y compone lo que hace falta aquí:
		  texto, tablas y las fuentes base del propio formato PDF, sin ningún .ttf del
		  sistema —que en la imagen nativa sobre musl no existe—. Las métricas de esas
		  fuentes van en el jar como recursos, así que NativeHints los registra.

		  Desde la versión 3 el paquete es org.openpdf.text (antes com.lowagie.text, la
		  herencia de iText); si algún ejemplo de por ahí no compila, suele ser eso.
		-->
		<dependency>
			<groupId>com.github.librepdf</groupId>
			<artifactId>openpdf</artifactId>
			<version>3.0.5</version>
		</dependency>

		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
	<dependency>
<groupId>org.springframework.boot</groupId>
<artifactId>spring-boot-starter-test</artifactId>
<scope>test</scope>
</dependency>
<dependency>
<groupId>org.springframework.boot</groupId>
<artifactId>spring-boot-resttestclient</artifactId>
<scope>test</scope>
</dependency>
<dependency>
<groupId>org.springframework.boot</groupId>
<artifactId>spring-boot-restclient</artifactId>
<scope>test</scope>
</dependency>
</dependencies>

	<profiles>
		<!--
		  Fully static native binary linked against musl (used by the Dockerfile together
		  with the "native" profile): no glibc at runtime, so the image runs on any x86-64
		  CPU. Binaries linked with the glibc of the build container (Oracle Linux 9) are
		  stamped as needing x86-64-v2 and fail to start on older CPUs / generic VM CPU
		  models with "CPU ISA level is lower than required".
		  Requires the GraalVM musl toolchain (ghcr.io/graalvm/native-image-community:25-muslib).
		-->
		<!--
		  Compilación rápida, para integración continua.

		  native-image tiene dos modos: el normal, que optimiza a fondo, y "quick
		  build" (-Ob), que tarda bastante menos y -lo que aquí importa- necesita
		  bastante menos memoria, a cambio de un binario algo más lento en
		  régimen. En el runner de GitHub (2 núcleos, 8 GB) el compilador pedía
		  más de 7 GB, llegaba a un pico de 7,6 GB y se pasaba el 38% del tiempo
		  recogiendo basura cuando no lo mataba el sistema directamente.

		  El intercambio es bueno para lo que esto es: una aplicación de gestión
		  para una comunidad de vecinos servida desde un NAS, donde el tiempo se
		  va en la base de datos y en la red, no en el código compilado. Para una
		  versión definitiva se compila sin este perfil.
		-->
		<profile>
			<id>native-quick</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.graalvm.buildtools</groupId>
						<artifactId>native-maven-plugin</artifactId>
						<configuration>
							<buildArgs combine.children="append">
								<buildArg>-Ob</buildArg>
							</buildArgs>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>

		<profile>
			<id>native-static</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.graalvm.buildtools</groupId>
						<artifactId>native-maven-plugin</artifactId>
						<configuration>
							<buildArgs combine.children="append">
								<buildArg>--static</buildArg>
								<buildArg>--libc=musl</buildArg>
							</buildArgs>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<!--
			  GraalVM native image. The Spring Boot parent's "native" profile wires the AOT
			  processing and binds native:compile to the package phase:
			      ./mvnw -Pnative -DskipTests native:compile   -> target/boxvault-backend
			  Needs a GraalVM JDK 25 (see Dockerfile / the GitHub Actions workflow).
			-->
			<plugin>
				<groupId>org.graalvm.buildtools</groupId>
				<artifactId>native-maven-plugin</artifactId>
				<configuration>
					<imageName>boxvault-backend</imageName>
					<buildArgs>
						<!-- Spanish month labels (StatisticsService) and dates: make es-ES the image's default locale -->
						<buildArg>-Duser.language=es</buildArg>
						<buildArg>-Duser.country=ES</buildArg>
						<!-- Fail the build on any class that would only break at runtime -->
						<buildArg>--no-fallback</buildArg>
						<!--
						  Portable CPU target. native-image defaults to x86-64-v3 (AVX2), which older
						  servers / some VMs do not offer ("CPU ISA level is lower than required").
						  "compatibility" runs on any x86-64 (and any AArch64) at a small speed cost;
						  use x86-64-v2 if the server is known to support SSE4.2/POPCNT.
						-->
						<buildArg>-march=compatibility</buildArg>
					</buildArgs>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<executions>
					<execution>
						<id>default-compile</id>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
					<execution>
						<id>default-testCompile</id>
						<phase>test-compile</phase>
						<goals>
							<goal>testCompile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
