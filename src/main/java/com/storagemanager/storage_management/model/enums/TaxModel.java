package com.storagemanager.storage_management.model.enums;

/** Tax returns the app helps with. */
public enum TaxModel {
    /** Quarterly VAT return. */
    MODELO_303,
    /** Yearly informative return of an entity in atribución de rentas. */
    MODELO_184,
    /** Yearly personal income tax (one per owner). */
    IRPF
}
